package model;

public class EntityTest {
    // I'm currently unsure what exactly we should be testing in relation to this!
    // Will look into this at a later stage as it might not make sense to do any testing in this specific stage.
}
